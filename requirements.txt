# ── Core orchestration ────────────────────────────────────────────────────────
langgraph>=0.2.0
langchain-core>=0.3.0
pydantic>=2.0.0
python-dotenv>=1.0.0

# ── UI ────────────────────────────────────────────────────────────────────────
streamlit>=1.42.0
pandas>=2.0.0

# ── Auth (Google sign-in via st.login) + per-user API key encryption ─────────
Authlib>=1.3.2
cryptography>=42.0.0

# ── Document ingestion ────────────────────────────────────────────────────────
pdf2image>=1.16.0
Pillow>=10.0.0
pytesseract>=0.3.10

# ── VLM: Google Gemini (FREE — recommended) ───────────────────────────────────
# Get your free API key at https://aistudio.google.com/app/apikey
# No credit card required — just a Google account
google-genai>=1.0.0

# ── VLM: Anthropic Claude (optional — paid API key required) ──────────────────
anthropic>=0.40.0

# ── Image preprocessing ───────────────────────────────────────────────────────
opencv-python-headless>=4.8.0

# ── Semantic vendor matching ──────────────────────────────────────────────────
rapidfuzz>=3.6.0
chromadb>=0.5.0

# ── Local VLM — Apple Silicon (mlx-vlm, no API key, no GPU required) ─────────
# macOS-only package — the sys_platform marker makes pip skip it on Linux
# (e.g. Streamlit Community Cloud), where it would otherwise fail to build.
mlx-vlm; sys_platform == "darwin"

# ── Local VLM — moondream2 (tiny ~2 GB, works on any hardware) ───────────────
# Already covered by transformers below
einops

# ── Local VLM — HuggingFace models (InternVL2 / LLaVA / moondream2) ─────────
# Uncomment if using local transformer-based models:
# torch>=2.1.0
# transformers>=4.40.0
# accelerate>=0.28.0
